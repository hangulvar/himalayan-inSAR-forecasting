# ============================================================================
# ACTIVE-AOI POINTER — this is the whole file.
# ----------------------------------------------------------------------------
# The pipeline reads this file by default (workflows/config.py). It points at
# one per-AOI registry file under config/ — switch every script to another AOI
# by editing this single line, or run one script against another AOI without
# switching via `--config config/<aoi>.yaml`.
#
#   Registry:   config/ramban.yaml · config/vaishnodevi.yaml · <your new AOI>
#   New AOI:    copy a registry file, then follow NEW_AOI_PLAYBOOK.md
#   Status:     python workflows/aoi_status.py  (all AOIs, all stages)
# ============================================================================
active_config: config/vaishnodevi.yaml
